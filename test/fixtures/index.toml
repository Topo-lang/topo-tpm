# A migrations/index.toml fixture — two consecutive version-to-version
# steps, exercising the multi-step chain of declaration-migration spec §4.2.

[[migration]]
from = ">=0.2.0, <0.3.0"
to   = "0.3.0"
rules = "0.2.0-to-0.3.0.migration.toml"

[[migration]]
from = ">=0.3.0, <1.0.0"
to   = "1.0.0"
rules = "0.3.0-to-1.0.0.migration.toml"
